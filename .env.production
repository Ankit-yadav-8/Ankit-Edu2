# Loaded by `vite build` (the production build Vercel runs).
# Only VITE_-prefixed vars reach the browser — this URL is public anyway.
# This tells the Vercel-hosted frontend where the Render backend API lives.
VITE_API_URL=https://ankit-edu2.onrender.com
